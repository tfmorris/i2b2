<project basedir="../" default="all" name="ONT Client">

	
	<property name="Project" value="edu.harvard.i2b2.eclipse.ontology" />

	<!-- ========== Executable Targets ======================================== -->
	<property name="Name" value="Ontology" />
	<import file="../../edu.harvard.i2b2.common/common_build.xml" />
	<property name="gensrc"     value="gensrc"/>

	<!-- =================================================================== -->
	<!-- 								INIT 								 -->
	<!-- =================================================================== -->
	 <target name="gensrcInit">
		<path id="classpath">
			<path refid="common-classpath" />
		</path>

		<taskdef name="xjc" classname="com.sun.tools.xjc.XJCTask">
			<classpath refid="classpath" />
		</taskdef>

	</target>



	<!-- =================================================================== -->
	<!-- 								CLEAN 								 -->
	<!-- =================================================================== -->
	<target name="clean"  description="Clean all build products" >
    	<delete dir="${dist}"/> 
    	<delete dir="${gensrc}"/>   
    	<delete dir="${genlib}"/>
    	<mkdir dir="${genlib}"/>
    	<mkdir dir="${dist}"/>
    	<mkdir dir="${gensrc}"/>
	</target>

	<!-- =================================================================== -->
	<!-- 								COMMON JAXB GENERATION 				 -->
	<!-- =================================================================== -->
	<target name="jaxb_gen" depends="gensrcInit" description="Generate JAXB classes for i2b2 and Patient Data messages">
		<xjc destdir="${gensrc}" removeOldOutput="no">
			<schema dir="../${i2b2xmlproject}/xsd/cell/ont" includes="ONT_LITE_QRY.xsd" />
			<schema dir="../${i2b2xmlproject}/xsd/cell/ont" includes="ONT_LITE_RESP.xsd" />
			<schema dir="../${i2b2xmlproject}/xsd/hive/msg" includes="i2b2_response.xsd" />
			<schema dir="../${i2b2xmlproject}/xsd/hive/plugin" includes="drag_drop.xsd" />
			<binding dir="." includes="etc/xsd/i2b2_vdo_bindings.xjb" />
			<binding dir="." includes="etc/xsd/i2b2_rspdo_bindings.xjb" />
			<binding dir="." includes="etc/xsd/i2b2_response_message_bindings.xjb" />
			<binding dir="." includes="etc/xsd/i2b2_dnd_bindings.xjb" />
		</xjc>
	</target>

	<!-- =================================================================== -->
	<!-- 								ALL 								 -->
	<!-- =================================================================== -->
	<target name="all" depends="jaxb_gen">
	</target>
</project>